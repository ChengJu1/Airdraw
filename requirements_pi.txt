# Raspberry Pi side dependencies (real-time capture / web drawing / exhibition app). Offline pipeline deps are in requirements.txt
# Deploy: copy capture_pi.py / web_capture.py / draw_app.py together with mgc3130.py, rt_filters.py
# to the same directory on the Pi, then pip install -r requirements_pi.txt
lgpio>=0.2
smbus2>=0.4
flask>=3.0          # web_capture.py only
google-genai>=1.0   # sketch recognition in web_capture.py only
pygame>=2.5         # draw_app.py only
